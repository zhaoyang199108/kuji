<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuji.dao.EduCounseDao">
	<insert id="insertIntoEduCounse" parameterType="com.kuji.entity.EduCounse">
		insert into eduCounse(eduCounse_title,eduCounse_content,eduCounse_img,exercise_id) values (#{eduCounseTitle},#{eduCounseContent},#{eduCounseImg},#{exerciseId});
	</insert>
	<select id="query" resultType="int">
        select count(1)  from eduCounse where  eduCounse_title=#{eduCounseTitle};
    </select>
    
    <select id="findAll" resultType="com.kuji.entity.EduCounse">
		SELECT
		t.eduCounse_id,
		t.eduCounse_content,
		t.create_time,
		t.eduCounse_title,
		t.eduCounse_img
		FROM `eduCounse` t;
	</select>
	
	   <select id="findEduCounseById" resultType="com.kuji.entity.EduCounse"
		parameterType="long">
		SELECT
		t.eduCounse_id,
		t.eduCounse_content,
		t.create_time,
		t.eduCounse_content,
		t.eduCounse_img
		FROM `eduCounse` t
		where t.eduCounse=#{id};
	</select>
</mapper>