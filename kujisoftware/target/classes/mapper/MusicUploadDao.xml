<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kuji.dao.MusicUploadDao">
	<insert id="insertIntoMusicUpload" parameterType="com.kuji.entity.MusicUpload">
		insert into musicUpload(musicUpload_type,musicUpload_playOrder,musicUpload_music)
		values(#{type},#{playOrder},#{music});
	</insert>
</mapper>